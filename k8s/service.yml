apiVersion: v1
kind: Service
metadata:
  name: appservice
  namespace: notesapp
spec:
  selector:
    app: notesapp
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000 

---

apiVersion: v1
kind: Service
metadata:
  name: mysqlservice
  namespace: notesapp
spec:
  selector:
    app: mysqlservice
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306